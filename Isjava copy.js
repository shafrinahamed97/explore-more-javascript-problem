// const my = 'file.js';
// console.log(my.endsWith('.js'));

function isJavaScriptFile(filecheck) {
    const myfile = myfile.endsWith('.js');
    if (myfile == true) {
        return true;
    }
    else {
        return false;
    }
}

const myfile = 'file.js';
console.log(myfile);